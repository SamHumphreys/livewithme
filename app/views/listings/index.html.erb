<h1>Listings#index</h1>

<% @listings.each do |listing| %>
  <% if listing.available? %>
    <p><%= link_to listing.address, listing  %></p>
  <% end %>
<% end %>

<p1><%= link_to 'Add New Listing', new_listing_path %></p1>
